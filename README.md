This project implements a deep learning-based 3D brain MRI registration framework using VoxelMorph to perform deformable alignment of individual brain scans to a standard MNI template.

The model is trained in an unsupervised manner and predicts dense deformation fields to spatially normalize MRI volumes. The framework enables standardized preprocessing for downstream neuroimaging analysis.
